/**
 * The player's spiritual alignment. The single source of truth lives in
 * MainScene via setPlayerPath(); content keys off this value.
 */
export type PlayerPath = 'neutral' | 'righteous' | 'corrupted';
